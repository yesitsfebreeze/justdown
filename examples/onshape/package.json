{
  "name": "onshape-playwright-example",
  "private": true,
  "version": "0.0.0",
  "description": "Runnable example: edit an Onshape CAD document via Playwright (UI) — see README",
  "scripts": {
    "test": "playwright test",
    "test:mock": "playwright test mock-editor",
    "test:selftest": "python3 onshape_edit.py --self-test",
    "test:endpoints": "python3 verify_endpoints.py",
    "verify": "python3 onshape_edit.py --self-test && python3 verify_endpoints.py && playwright test mock-editor",
    "auth": "playwright codegen --save-storage=auth.json https://cad.onshape.com/signin"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
